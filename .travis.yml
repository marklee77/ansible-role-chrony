---
language: python
python: "2.7"

before_install:

    - sudo apt-get update

install:

    # Install Ansible.
    - sudo apt-get -y install ansible
    - sudo apt-get -y install strace
    - ansible-playbook --version
    - sudo ntpdate pool.ntp.org

script:

    # Check the role/playbook's syntax.
    - ansible-playbook -i travis/inventory site.yml --syntax-check

    # Run the role/playbook with ansible-playbook.
    - ansible-playbook -i travis/inventory site.yml

    # Run the role/playbook with ansible-playbook.
    - ansible-playbook -i travis/inventory site.yml

    # properly idempotence should require no changes, but the travis vm seems to
    # have trouble seeing that chrony is running...
    - >
      ansible-playbook -i travis/inventory site.yml
      | grep -q 'changed=1 .*failed=0'
      && (echo 'Idempotence test: pass' && exit 0)
      || (echo 'Idempotence test: fail' && exit 1)

    - sudo ps aux | grep chrony

    - sudo grep chrony /var/log/syslog | grep dump
