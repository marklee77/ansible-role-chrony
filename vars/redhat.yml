chrony_package_info:
  pkg_mgr: yum
  args:
      name: "{{ item }}"
      state:  "{{ chrony_pkg_state }}"
  pre_pkgs: []
  pkgs:
    - chrony

chrony_path_info:
  conf_path: /etc/chrony.conf

chrony_service_info:
  name: chronyd
